# 💬 Real-Time Styled Chat App (Node + Express + EJS + PostgreSQL)

A sleek animated chat application built using **Node.js, Express, EJS, PostgreSQL, HTML, CSS & JS (Polling-based real-time updates)**.

This version includes:

- ✔ Modern UI (Dark + Neon Theme)
- ✔ Message animations (pop-in, fade)
- ✔ Typing indicator
- ✔ Send & Receive sounds
- ✔ Auto scroll
- ✔ No WebSockets — Simple polling approach
- ✔ Postgres database for message storage

---

## 🚀 **Tech Stack**

| Layer | Technology |
|-------|------------|
| Frontend | HTML, CSS, Vanilla JS, EJS |
| Backend | Node.js + Express |
| DB | PostgreSQL |
| Real-time Style | Polling (`setInterval`) |
| Template Engine | EJS |

---

## 📦 **TO RUN THE APP**

```bash
git clone https://github.com/OmikAgrawal/chat-app.git
cd chat-app
npm i
nodemon server.js
